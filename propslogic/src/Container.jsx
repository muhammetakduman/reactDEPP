import React from 'react'

function Container({ children }) {
    console.log()
    return (
        <div>
            conainet aktiff
            {children}
        </div>
    )
}

export default Container
